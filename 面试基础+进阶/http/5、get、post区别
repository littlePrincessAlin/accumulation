GET 用于获取资源，POST 用于提交资源。

（1）缓存
get浏览器会主动缓存，post不能
（2）参数
get的参数会在url上，post则是在包体里，相对get安全；get传递的数据量比post小；
URL有长度限制，会影响 Get 请求，但是这个长度限制是浏览器规定的；
（3）GET请求幂等，POST请求不幂等
幂等：发送 M 和 N 次请求，服务器上资源的状态一致。
（4）请求报文
GET请求会一次性发送请求报文；POST请求通常分为两个TCP数据包，首先发 header 部分，如果服务器响应 100(continue)， 然后发 body 部分。
