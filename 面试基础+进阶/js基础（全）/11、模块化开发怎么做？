知道什么是模块化开发？
1、模块就是一个有特定功能的文件，可以通过加载模块，得到特定的功能；
2、模块化开发就是js的功能分离，通过需求引入不同的文件；
3、模块化开发可以使代码耦合度降低，避免代码多次出现在页面；
模块开发要遵循的规范？
 AMD规范： 异步模块加载， 使用define定义模块,使用require调用模块
 CommonJS规范：服务器端模块的规范,node.js就采用了这个规范
 CMD规范： 按需加载
 Es6：